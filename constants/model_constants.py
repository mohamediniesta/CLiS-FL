NUM_CHANNELS = 1  # ? Number of channels in the model.
NUM_CLASSES = 10  # ? The number of classes in the dataset.
LR = 0.1  # ? Learning rate.
MOMENTUM = 0.5  # ? Optimization function.
LOCAL_BS = 10  # ? Local batch size.
BATCH_SIZE = 32  # ? Batch size.
